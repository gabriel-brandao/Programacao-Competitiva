#include<stdio.h>
int main(){
    int N, i, j;

    scanf("%d", &N);
    char C[N][100];

    for(i=0; i<N; i++){
        fflush(stdin);
        scanf("%[^\n]s", &C[i]);
}
        printf("Ciencia da Computacao\n");
}
